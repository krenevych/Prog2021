N = int(input("Введіть порядок визначника "))

t = 2  # поперідній член послідовності
d = 1  # поточний член послідовності

for n in range(3, N + 1):
    d, t = 2 * d - 3 * t, d

print(d if N >= 2 else 2)
